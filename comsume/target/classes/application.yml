eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: comsume-client
feign:
  hystrix:
    enabled: true
management:
  endpoint:
    health:
      show-details: always